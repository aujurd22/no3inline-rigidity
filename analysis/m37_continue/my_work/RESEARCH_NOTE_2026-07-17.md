# m=37 rot4-NTIL — 独立延续研究笔记 (2026-07-17)

> 源目录 `C:\Users\djr82\Documents\Codex\...\work\ising_m37` 只读镜像已复制到
> `analysis/m37_continue/`（未改动源）。本笔记记录我方在自有工作区内的独立推进。
> **按用户要求：本阶段产出暂不进 README、暂不推送 GitHub**，等外部 agent 研究收尾后统一整合。

## 0. 已建工具（自有、与源无关）

| 文件 | 作用 |
|---|---|
| `my_work/my_verify.py` | **独立地面真值**：148 点行列式暴力数共线三元组 `brute_bad`；2-因子合法性 `is_2factor`；对角线不变量硬筛选 `diagonal_unsafe`（取向无关）。 |
| `my_work/search_push.py` | 新搜索：多种子加权-Ising LNS（含 2-orbit），k-交换扰动 + 随机重启，每个接受候选用 `brute_bad` 复核。后台运行中。 |
| `my_work/run_shell_extension.py` | 扩展 v40_02 匹配壳层到未探距离（先填 d=4 缺口、再探 d=7 前沿）。后台运行中。 |

## 1. 独立验证结果（地面真值交叉确认）

`my_verify.brute_bad` 对外部 agent 档案中各档最佳配置计数，与其报告值**完全吻合**：

| id | 报告值 | 我的 brute_bad | 对角线安全 |
|---|---|---|---|
| v40_01 | 40 | 40 | True |
| v40_02 | 40 | 40 | True |
| v48_01 | 48 | 48 | True |
| v52_01 | 52 | 52 | True |
| v56_01 | 56 | 56 | True |
| v64_01 | 64 | 64 | True |

`diagonal_unsafe` 双向校验：对我们的旧 `config_408`（外部已证对角线不安全）返回 **True**；
对上述 40/48/52/56/64 配置返回 **False**（安全）。两方向均正确 ⇒ 筛选器可信。

## 2. 关键结构发现：56-极小匹配的盆地边界

v40_02 的 13-自由边邻域匹配壳层（固定其余 24 边）已完整枚举至距离 6：

| 距离 d | 枚举是否闭合 | 最小几何坏三元组 |
|---|---|---|
| 2 (2-交换) | 是 | **64** |
| 3 | 是 | **56** |
| 4 | （缺口，本笔记补） | ≥60 (p0 实测) |
| 5 | 是 | **56** |
| 6 | 是 (p8 分区) | **56** |

**唯一的 d=3 56-极小匹配**是一个干净的 **6-圈 3-交换**：
- 顶点 {1,10,17,19,24,28}
- 原边 (1,19)(10,24)(17,28) → 新边 (1,24)(10,28)(17,19)
- 即 6-圈 1–19–17–28–10–24–1 的两条完美匹配交替翻转。

含义：从 v40_02 出发，任何扰动其 13 自由边（距离 ≥2）至少新增 **4 个缺陷轨道**
（56−40=16 = 4×4），最浅的逃出口恰是上述 6-圈翻转。v40_02 是一个**深而窄的井**：
即便重排全部 13 自由边（d≥2），值仍 ≥56。要低于 40，必须改动那 24 条固定边之一。

> 这是「局部最优性」的精确几何刻画，但目前仍是**局部**证书（仅针对 v40_02 的
> 24-固定邻域）。它不构成对「所有 37 阶 2-因子」的全局下界。

## 3. m=37 当前状态（综合）

- 最佳已知几何坏三元组 = **40**（4 个独立盆地：v40_01..04，3 种循环型）。
- 4-整除定理：bad ≡ 0 (mod 4) ⇒ 阶梯 40 → 36 → 32 → … → 0。下一档为 **36**。
- 对角线不变量：真解必须对角线安全（config_408 因对角线不安全被剔除）。
- 全局是否可降于 40：**OPEN**。尚未有普适下界证明「任意 37 阶 2-因子 ≥ X」。

## 4. 全局定理需要什么（研究纲领，未解决）

匹配壳层本质是**局部**的。要证明「m=37 无解」或「m=37 最小缺陷 ≥ k」，必须处理
**全部** 37-边 2-因子，而非单个构型的邻域。候选路线：

1. **模块化/素数障碍（R9 系列延伸）**：m=37 是素数。在 mod-37 算术下，C4 提升点
   落在特定剩余类；若存在线性代数依赖强制某线 ≥3 点，则给出普适下界。需构造
   具体的 mod-37 线性约束并证其不可满足（类似 R9a 的素数剪枝，但升为证明）。
2. **联合 LP/SDP 松弛（路线 3）**：对所有 2-因子的加权-Ising 目标做联合松弛，
   求普适下界。每构型 SDP 已给 13–107，但非普适证书。
3. **禁止模板提升（由 §2 推广）**：把「6-圈 3-交换 ⇒ +4 缺陷轨道」这类局部观察，
   抽象为「任意 FDR-可行 k-交换 ⇒ 至少新增 4·? 缺陷轨道」的静态割，再尝试覆盖
   全部 k。目前仅对 v40_02 的单一 6-圈成立，未普适。
4. **小 m 归纳**：在可穷举的小 m 上穷举最小缺陷，寻找随 m 的单调性/相变规律，
   外推 m=37。m≤36 均有解（缺陷 0）；m=37 是首个未知点。

## 5. 正在运行的实验（后台）

- **search_push.py**：多种子（4 个 v40 盆地 + 400 个随机 2-因子）+ k-交换 LNS
  （k 自适应至 14）+ 随机重启，目标 <40。每接受候选用 `brute_bad` 复核。
  预算 3h。检查点 `my_work/best_push.json`。
- **run_shell_extension.py**：填 d=4 缺口（p4）+ 探 d=7 前沿（p8），每分区枚举至
  闭合。完成 `my_work/shell_extension_summary.json`。

## 6. 待外部 agent 收尾后整合进 README/SIRH 的要点（暂不执行）

- 加权 Ising（含 2-orbit 修正）取代旧 `enumerate_clauses` 的 3-胞仅模型。
- 对角线不变量 = SIRH Part III 在斜率±1 方向的实例化 + 廉价外层剪枝定理。
- 4-整除定理（bad ≡ 0 mod 4）作为新引理。
- config_408（64）标注「对角线不安全 ⇒ 永非真解」，解释长期卡 64 的根因。
- m=37 状态表：最佳 40（4 盆地），局部匹配壳层证明 v40_02 井深至 56。
