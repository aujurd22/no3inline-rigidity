"""
fallback_strategy.py — 备用策略：如果快筛找不到 GV < 70 的配置

策略 A: 穷举 [28,9] 的最大独立集配置
  从一个 28-圈和一个 9-圈开始 —— 它们是互补的。
  C(37,28) ≈ 3.7×10^9 种选择，太多了。
  但我们可以固定 28-圈的顶点集，只变排列。

策略 B: 使用 m=36 的解嵌入 vertex 36
  m=36 时有解，嵌入 vertex 36 后第 37 个顶点的两条边
  可以选 36 种不同的连接方式。
  每种产生一个新的 37-顶点 2-因子。

策略 C: 直接构造法
  设 S 是 37 个 cell 的坐标集合。
  相邻 cell 共享顶点 —— 在 C4 空间中，共享顶点意味着
  两个 C4 钻石的交集可能产生共线。
  构造一个 2-因子使得共享顶点间距离最大化。

策略 D: 使用 simulated annealing 直接优化 2-因子（边空间）
  步骤：
  1. 从 config_408 开始
  2. 随机 2-边交换
  3. 如果新配置的 GV 更低 → 接受
  4. 如果 GV 更高 → 以概率 p 接受（退火）
  5. 每 100 步降温
  6. 找到的每个局部最小值用 CP-SAT 验证

这是目前最合理的方向。已在 incremental_search.py 中实现。
但 GV 计算太慢（0.278s/配置）限制了搜索规模。

优化点：
- 用 numpy 加速 collinearity 检查
- 使用增量 GV 更新（只重新计算涉及交换的 4 cell 的三元组）
- 从 0.278s → 理论可降至 ~0.03s/次交换 = 30000 次/15min
"""
print("Fallback strategy planned — see comments above")
