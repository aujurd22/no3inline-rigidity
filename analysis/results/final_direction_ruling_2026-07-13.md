# 方向①~⑤ 最终裁定（2026-07-13 收口）

基于今日所有实验与结果，五条方向全部完成评估。

---

## ❌ 方向① LLL/熵压缩（已穷尽）

| 子路线 | 结果 |
|---|---|
| Vanilla LLL 在单随机 2-因子空间 | `ep(d+1)>1` (m≥14), **失败**(已证明必然失败) |
| Moser–Tardos 随机重采样 | m=10 仅 2.7% 解出率, 大 m 外推更差 |
| csearch 贪婪下降 (ys-swap 邻域) | 精确增量正确但因邻域受限卡在 `best=4/8` |
| csearch 随机 SA (坐标互换) | 正确性已修, 但邻域(仅交换 y 坐标)太小 |

**根因**：csearch 使用的 `ys` 交换空间远小于完整 2-因子边交换空间。gating 证明的 "red_config_frac=1.0" 存在于完整边交换空间，但 csearch 只爬了它的一个子集，而该子集不连通。

---

## ❌ 方向② m=36 手术（已评估，已排除）

**关键发现**：m→m+1 时 C4 几何发生根本性变化。C4 旋转公式 `(x,y) → (N-1-y, x)` 依赖 `N=2m`。当 m=36→37（N=72→74），同样的 cell (x,y) 产生不同的 C4 提升点：

- m=36 解（36 cells, N=72）：0 条坏线 ✓
- 同样 36 cells 用 N=74 提升：**152 条坏线** ✗

即种子解本身在 m=37 的几何框架下无效——因为旋转中心从 (35.5,35.5) 移到 (36.5,36.5)，所有点的位置都变了。

**穷举搜索**（v1–v5 共 4 次迭代）：
- 36 种 1-edge 分割 + 630 种 2-switch+分割 + 全 3-switch 枚举 + 50000 随机尝试 → **0 解**
- 不是搜索空间不够大，而是基础假设（种子解可携带）不成立

**结论**：方向②的"有界手术"路线因 C4 几何的 N 依赖而**不可行**。从 m 的解构造 m+1 的解等价于从头求解。

---

## ❌ 方向③ Lazy CP-SAT（已测，排除）

**决定性实验**：
- m=17：30 迭代, 506 约束 (0.9%), 总时间 15.0s
- 每迭代 <0.5s, 但 viol 在 48-100 间震荡 (无下降趋势)
- 全量模型 120s 仍 UNKNOWN, 但 lazy 也不收敛

**根因**：每线约束只消除极小部分搜索空间，增量添加无法迫使求解器趋向正确解——这是组合约束与连续优化的本质差异。

---

## ❌ 方向④ 能量下界（已系统评估，未突破）

**已完成的分析**：
1. **E3 方向键空间**：方向键总数 O(m³) ⨠ C(4m,2) ≈ 8m²，ratio→0（m=37: 0.0004）。非瓶颈
2. **E4 C4-轨道分析**：C4-轨道总数 O(m³) ÷ 4 ⨠ C(4m,2)/4，ratio→0（m=14: 0.003, 外推 m=37: <0.001）。非瓶颈
3. **方向键单射性**：等价于 NTIL 定义本身，非新约束
4. **奇偶模 2 分析**：行列式恒为偶数，无非平凡模 2 约束
5. **对角约束定理**：至多 1 cell 在 x=y 上（在所有已知解中验证，但不区分 m）
6. **模 p 约束**：m=37 为素数，F_37 上 C4 旋转结构不同，但未找到可证明的障碍
7. **二次型零点覆盖**（代数几何路线）：理论上仍有希望，但需完整的 Gröbner 基，超出本框架

**结论**：所有计数/鸽笼类论证不适用（容量过剩）。代数几何路线可能是最后的理论希望。

---

## ✅ 方向⑤ 数据驱动猜定理（已测，排除了关键错误）

- dir5 脚本已写，但 JSON 半截截断需修（已修复原子写）
- 关键贡献：通过数据分析提前修正了 "mutual edge 被基帧共线禁止" 的朴素猜测
- 未发现有潜力直接解决 m=37 的新不变量

---

## 最终计算路线状态

| 路线 | 状态 | 原因 |
|---|---|---|
| CP-SAT 全量模型 | ❌ m≥17 presolve 墙 | 126 万约束使构建/预处理卡死 |
| CP-SAT Lazy (方向③) | ❌ 不收敛 | 增量约束无法迫使求解器趋向正确解 |
| csearch SA/贪婪 (方向①) | ❌ 邻域不足 | ys-swap 空间远小于完整 2-因子边交换空间 |
| 随机重采样 / Moser–Tardos | ❌ 解出率极低 | m=10 仅 2.7% |
| 代数族 (幂函数/dlog/Welch) | ❌ R9g 全失败 | 太共线 (基帧 3-free 不满足) |

---

## 结论：m=37 求解问题在现有理论和算法下不可解

这是本项目的**真实收口**。所有已知的计算路线和理论路线（SIRH Part I–IV、R9a–h、方向①–⑤）均不能解决 m=37 开放问题。

**项目遗留价值**：
1. **SIRH 理论框架**（Part I–IV）—— 对称诱导的分层代数刚性，完整分类了所有 FDR C4 对称 NTIL 的线性/二次/全局必要条件
2. **R9b Th-44** —— rot4 NTIL ⇔ 2-正则图的精确编码
3. **R9e 障碍密度分析** —— E[B]≈10.3m, 16 取向二次型障碍签名
4. **gating 全量数据** —— 30 个 m 值的 p/d/ep(d+1)/red_config_frac 完整测量
5. **Cycle-type 环型细化 T15** —— 3-环自安全证明、2-环非结构禁止、30 m 全统计
6. **csearch 正确性修复** —— 64-bit line_of 打包、持久化 lpts 精确增量维护、parser 修复

**更新 (19:03)：Burnside 精确轨道计数 + 单37环/terrace理论 完成。**
- Burnside 轨道计数：计算出各对称群的方向键轨道精确数 → C4 压缩比 0.263, D4 0.125（N=74）
- R8-G "28常数" 误导声明已修正为精确 m 依赖公式
- 单37环 + terrace/starter理论：将 (X)+(S) 约束改写为相邻差分的禁条件
- Terrace 自动满足 Sidon（饱和）→ 仅需验证 (X) 行列式条件
- 4 个显式 terrace 族已鉴定（二次剩余、乘法生成元、Skolem型、交错 starter）
- 方向B mutual-edge 测试完成：518 配置均未找到 NTIL 解

**可能的下一突破方向（超出当前框架范围）**：
- 完整 2-因子空间上的有效边交换局部搜索（非坐标互换）— csearch2 已验证 best 4 vs 32 (8×提升)
- 代数几何 / 行列式分解的代数证明（当前理论缺口）
- 全新数学模型（完全避开几何共线判定）

---

*记录时间：2026-07-13 16:30 (方向④全部系统分析完成，所有5方向收口)*
